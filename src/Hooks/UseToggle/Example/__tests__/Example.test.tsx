
import Example from '..'

describe('useToggle example unit test cases', () => {
    // it('Should not render Modal Body initially', () => {
    //     render(<Example />);
    //     const bodyElement = screen.queryByText('Modal Body');
    //     expect(bodyElement).not.toBeInDocument();
    // });
})